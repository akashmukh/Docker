pipeline {
    agent any 
    
    stages{
       stage('test'){
          steps{
                sh 'docker version'
                sh 'docker pull nginx'
            }
          }
        }
      }
